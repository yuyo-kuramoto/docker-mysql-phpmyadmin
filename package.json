{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && swc ./src -d dist",
    "db:migrate": "npx prisma migrate dev --name init",
    "docker:db:migrate": "docker compose run nodejs npm run db:migrate",
    "docker:prisma:generate": "docker compose run nodejs npx prisma generate",
    "rebuild:be": "docker compose build nodejs && docker compose rm --stop nodejs && docker compose up -d nodejs",
    "start": "node dist/index.js",
    "start:docker": "npm run build && node --inspect=0.0.0.0 dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.14",
    "@types/express": "^4.17.14",
    "@types/morgan": "^1.9.3",
    "@types/node": "^18.11.9",
    "prisma": "^4.6.1",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@prisma/client": "^4.6.1",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "nanoid": "^3.1.30"
  }
}
